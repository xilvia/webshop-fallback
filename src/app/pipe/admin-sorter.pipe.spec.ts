import { AdminSorterPipe } from './admin-sorter.pipe';

describe('AdminSorterPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminSorterPipe();
    expect(pipe).toBeTruthy();
  });
});
