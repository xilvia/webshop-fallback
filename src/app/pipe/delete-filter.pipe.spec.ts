import { DeleteFilterPipe } from './delete-filter.pipe';

describe('DeleteFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DeleteFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
